server:
  port: 8080
  servlet:
    context-path: "/products"

spring:
  application:
    name: ProductService
  datasource:
    url: "jdbc:mysql://localhost:3306/dbproduct"
    username: "root"
  jpa:
    show-sql: true